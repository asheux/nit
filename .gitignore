target/
**/*.rs.bk
**/*.swp
**/*.swo
.DS_Store
.idea/
.vscode/
__pycache__/
gol-snapshots/
games-runs/
output/
runs/
# Local research notebooks / scratch (Wolfram .nb, experiments) — not shipped.
research/
*.log
*.tmp
perf.data
perf.data.old
flamegraph.svg
.nit/
vendor/
!vendor/time/
!vendor/time/**
# Vendored tree-sitter-wolfram grammar (no crates.io release); must be tracked
# so a fresh checkout can build the nit-syntax path dependency.
!vendor/tree-sitter-wolfram/
!vendor/tree-sitter-wolfram/**
.cargo-local/
.claude/
__pycache__/

# Local secrets / environment — never commit real credentials.
# (.env.example is intentionally NOT ignored so it can be committed.)
.env
.env.local
.env.*.local
*.pem
*.key
*.p12
*.pfx
*.keystore
.netrc
secrets.json
credentials.json
