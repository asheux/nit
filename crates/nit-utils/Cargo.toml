[package]
name = "nit-utils"
version = "0.1.0"
edition = "2021"
rust-version.workspace = true
description = "Shared filesystem, hashing, path, and time utilities for the nit workspace"
license = "MIT"
publish = false
categories = ["filesystem", "development-tools"]
keywords = ["utilities", "hashing", "atomic-write"]

[dependencies]
blake3 = { workspace = true }
directories = { workspace = true }
