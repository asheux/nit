[package]
name = "nit-utils"
version = "0.1.0"
description = "Shared filesystem, hashing, path, and time utilities for the nit workspace"
edition = "2021"
license = "MIT"
rust-version.workspace = true
publish = false

[dependencies]
directories = { workspace = true }
blake3 = { workspace = true }
