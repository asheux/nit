//! Agent-lane / mission / console types.
//!
//! Currently a stub: the actual definitions still live in `state.rs` because
//! moving them requires updating ~30 type relocations plus the `lib.rs`
//! re-export list while preserving serde tags and field order. Tracked in the
//! shard's risks JSON for a future dedicated turn.
