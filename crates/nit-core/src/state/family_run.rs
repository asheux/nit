//! Family-run config builders, FSM/TM/CA generators, parsers, and disk cache.
//!
//! Stub: the 820-line family-run subsystem still lives in `state.rs`. The
//! 5 `pub fn build_family_run_override*` entry points are re-exported via
//! `lib.rs:70-71`, so any move must keep those paths resolving. Deferred to
//! a dedicated turn; tracked in the shard's risks JSON.
