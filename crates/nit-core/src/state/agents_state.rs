//! `AgentsState` struct + impl + Default.
//!
//! Stub: the 750-line `AgentsState` struct + `default_with_mocks` + `Default`
//! impl still live in `state.rs`. Moving them requires cross-referencing the
//! 7 serde-default helpers in `state/defaults.rs` plus the agent-types defs
//! and is a dedicated-turn task. Tracked in the shard's risks JSON.
